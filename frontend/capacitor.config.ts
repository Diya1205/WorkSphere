import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.worksphere.hrms',
  appName: 'WorkSphere HRMS',
  webDir: '.output/public',
};

export default config;
